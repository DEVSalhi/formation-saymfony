{% extends '/admin/base.html.twig' %}

{% block title %}Administration des annonces !{% endblock %}

{% block body %}

    <div class="container-fluid">
        <h1 class="my-5">
            Gestion des annonces
        </h1>
        <table class="table table-hover">
            <thead>
                <tr>
                    <th>ID</th>
                    <th>Titre</th>
                    <th>Auteur</th>
                    <th>Réservation</th>
                    <th>Note</th>
                    <th>Action</th>
                </tr>
            </thead>
            <tbody>
             {% for ad in pagination.data  %}
                <tr>
                    <td>{{ ad.id }}</td>
                    <td>{{ ad.title }}</td>
                    <td><img src="{{ ad.author.picture }}" alt="" class="avatar avatar-mini mr-2"> {{ ad.author.fullName }}</td>
                    <td> <span class="badge badge-primary rounde">{{ ad.bookings | length }}</span> </td>
                    <td><span class="badge badge-primary rounde">{{ ad.avgratings | number_format(1,',',' ')}}</span></td>
                    <td>
                        <a href="{{path('admin_ads_edit',{'id':ad.id})}}" class="btn btn-primary">
                            <i class="fas fa-edit"></i>
                        </a>
                        <a href="#" class="btn btn-danger">
                            <i class="fas fa-trash"></i>
                        </a>

                    </td>
                </tr>

            {% endfor %}
            </tbody>
        </table>
        {{ pagination.display }}
    </div>

{% endblock %}
